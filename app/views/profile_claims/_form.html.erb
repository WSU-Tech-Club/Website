<%= form_for([@profile_claim.profile,@profile_claim]) do |f| %>
  <%= f.hidden_field :profile_id, :value => params[:profile_id] %>
  <% if @profile_claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_claim.errors.count, "error") %> prohibited this profile_claim from being saved:</h2>

      <ul>
      <% @profile_claim.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
